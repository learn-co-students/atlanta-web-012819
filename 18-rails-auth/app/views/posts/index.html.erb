<h1>Blog Posts...</h1>

<ul>
    <% @posts.each do |post| %>
        <li><%= link_to(post.title, post_path(post)) %></li>
    <% end %>
</ul>

<% if current_user %>
  <%= link_to("Add post", new_post_path) %>
<% end %>

<br/>

<%= link_to("Sign up", new_user_path) %>
